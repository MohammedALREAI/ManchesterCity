import React from 'react'
import {Route,Redirect} from'react-router-dom'
export const PrivateRoute = ({

     user,
     component:Com,
     ...rest
}) => {
     return (
          <div>

          </div>
     )
}

